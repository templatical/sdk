{
  "name": "templatical",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --filter '@templatical/media-library' build && bun run --filter '@templatical/types' build && bun run --filter '@templatical/core' --filter '@templatical/renderer' --filter '@templatical/import-beefree' build && bun run --filter '@templatical/editor' build",
    "build:email-editor": "vite build --config vite.email-editor.config.ts",
    "build:media-library": "vite build --config vite.media-library.config.ts",
    "build:all-sdk": "bun run build:email-editor && bun run build:media-library",
    "test": "bun run --filter './packages/*' test",
    "test:coverage": "bun run --filter './packages/*' test -- --coverage",
    "lint": "eslint packages/*/src apps/*/src",
    "format": "prettier --write 'packages/*/src/**/*.{ts,vue}' 'apps/*/src/**/*.{ts,vue}'",
    "typecheck": "bun run --filter './packages/*' typecheck",
    "ci": "bun run format && bun run lint && bun run typecheck && bun run test"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "@tailwindcss/vite": "^4.2.2",
    "@templatical/editor": "workspace:*",
    "@tiptap/extension-color": "^3.22.1",
    "@tiptap/extension-font-family": "^3.22.1",
    "@tiptap/extension-text-style": "^3.22.1",
    "@vitejs/plugin-vue": "^6.0.5",
    "@vitest/coverage-v8": "^4.1.2",
    "@vue/reactivity": "^3.5.31",
    "eslint": "^10.1.0",
    "eslint-plugin-vue": "^10.8.0",
    "prettier": "^3.8.1",
    "typescript": "^6.0.2",
    "typescript-eslint": "^8.58.0",
    "vite": "^8.0.3",
    "vue-eslint-parser": "^10.4.0"
  },
  "dependencies": {
    "@vueuse/core": "^14.2.1",
    "liquidjs": "^10.25.2",
    "vue": "^3.5.31",
    "vue-advanced-cropper": "^2.8.9"
  }
}
