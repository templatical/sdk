# These docs are public and intentionally machine-readable: /llms.txt is a
# curated index, and every page is also served as raw markdown beside its
# rendered HTML — /guide/theming.md for /guide/theming, and /backend/index.md
# for a directory URL like /backend/, which has no bare .md form. Crawling is
# allowed on purpose — this is a source-available SDK, and an agent that has
# read the docs gives better answers about it than one guessing from a model's
# training data.
User-agent: *
Allow: /
Disallow: /cloud/
Disallow: /de/cloud/

Sitemap: https://docs.templatical.com/sitemap.xml
